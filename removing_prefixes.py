# Run this practice in terminal

nostarch_url = 'https://nostarch.com'
nostarch_url.removeprefix('https://')
print(nostarch_url.removeprefix('https://'))  # Only use print when not running code in the terminal